export const places = (state) => state.places;
export const errors = (state) => state.errors;
export const filters = (state) => state.filters;
export const paginator = (state) => state.paginator;
export const allowedFilters = (state) => state.allowedFilters;
export const count = (state) => state.count == null ? 0 : state.count;
